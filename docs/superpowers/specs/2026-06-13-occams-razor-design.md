# 奥卡姆剃刀原则 — 设计规格

> 为 `python-code-format/SKILL.md` 新增"奥卡姆剃刀原则"章节

## 目标

将"最简单的方案往往是对的"作为独立原则写入代码格式规范，包含原则声明、判据和正反对比例子，覆盖数据结构、算法、抽象层级、代码组织四个维度。

## 插入位置

放在"函数长度与复杂度参考"之后、"代码质量检查规则"之前。作为代码质量维度的补充规则。

## 新增章节内容

### 1. 原则声明

```markdown
### 奥卡姆剃刀原则：最简单的方案往往是对的

在选择实现方式时，优先选择**最朴素、最直觉的方案**，除非有明确的性能数据或正确性证据要求你选择更复杂的路径。

判断"更简单"不是凭感觉，而是多维度比较：
- **代码行数更少**（在可读性不下降的前提下）
- **引入的抽象更少**（少定义一个类层级、少创建一个中间抽象）
- **依赖更少**（少用一个第三方库、不引入不必要的模块或外部服务）
- **执行路径更短**（更少的 if/else 分支、更少的中间跳转）

复杂度应该是"被证明需要"之后才引入的，而非"万一将来需要"就提前铺设。
```

### 2. 数据结构选择（pandas vs 标准库示例）

### 3. 算法与实现路径选择（reduce+lambda vs 生成器表达式示例）

### 4. 抽象层级选择（不必要 ABC 基类示例）

### 5. 代码组织与拆分（过度拆分子函数示例）

## 设计决策

- **位置**：放在"函数长度与复杂度参考"之后，与该节形成递进关系（函数内部复杂度 → 整体设计复杂度）
- **风格**：与文档现有风格一致——英文标点、每个小节一个对比示例、判据明确
- **范围**：覆盖函数/模块级别，不涉及系统架构级决策（超出本规范定位）
